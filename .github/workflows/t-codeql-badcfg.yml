name: t codeql badcfg
on: workflow_dispatch
permissions:
  contents: read
  security-events: write
  actions: read
  packages: read
jobs:
  codeql:
    uses: tznthou/deepseek-code-review/.github/workflows/reusable-codeql.yml@v1
    with:
      languages: '["python"]'
      kit-ref: v1
      config-file: .github/does-not-exist.yml
      skip-trivy: true
