name: ai review post

on:
  workflow_run:
    workflows: ['ai review collect'] # 必須與上面那支的 name: 完全一致
    types: [completed]

jobs:
  post:
    uses: tznthou/deepseek-code-review/.github/workflows/reusable-ai-review-post.yml@v1
    secrets:
      DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
