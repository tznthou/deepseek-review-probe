name: ai review collect

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  collect:
    uses: tznthou/deepseek-code-review/.github/workflows/reusable-ai-review-collect.yml@feat/reusable-workflows
